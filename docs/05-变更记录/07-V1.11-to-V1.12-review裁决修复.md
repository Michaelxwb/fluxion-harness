# V1.11 → V1.12：全库 Review 裁决修复

> 背景：对 docs/ 全库做了四轮深度 review（文档一致性、总设承接度、交互稿专项），按用户裁决执行修复。本版不引入兼容方案，直接以裁决结果改写。

## 用户裁决

1. Schema 权威：以《08-数据库表所有权》+ 模块 design-full 为唯一权威；《05-数据库公共字段与表清单》(V1.9.1) 降级为历史参考。
2. Channel Gateway 技术栈：Node.js/TypeScript + Fastify + @wecom/aibot-node-sdk（接企微官方 IM 插件）。
3. Human Checkpoint：补回全链（状态机 → API → 前端）。
4. Knowledge：V1 不交付，仅规划。

## 修改清单

### 00-总体设计
- 总设 §4.2 关键索引：删除废弃幽灵表 external_auth_profile/user_service_auth；channel_identity 唯一键统一为 `UNIQUE(tenant_id, channel_type, identity_scope_key, external_user_id)`；capability_definition 唯一键改 `(tenant_id, key)`。
- 总设 §6.1：Knowledge Runtime + SPI 由"必须"改为"规划项（V1 不交付）"。
- 总设 §6.6 IA：菜单清单补"审计查询（Admin）"。
- 总设 §6.4 变更控制表补 V1.8~V1.11 行。
- Playbook §9.6：Snapshot 范围改为"不 pin Capability Implementation"，与总设 §4.2 对齐。

### 01-架构与规范
- 05-数据库公共字段与表清单：文头加历史快照降级声明；README 同步标注。
- 08-表所有权：service_release/execution_snapshot content_hash 唯一改 `(tenant_id, service_id[, service_release_id], content_hash)`；skill_artifact 改 `(tenant_id, skill_id, checksum)`；service_execution 补 waiting_reason/human_deadline/requested_action/channel_source/retry_count/priority；新增 channel_delivery 表（owner 模块 10）。
- 04-数据库设计基线：事务边界更新为 channel_binding 拆分模型；UserPlatformCredential 表名更正为 UserProjectCredential。
- 06-接口设计基线：short_page_terminates 默认值裁决为 true。

### 02-模块设计
- 模块 01：新增 CORE-LIB-04 ExecutionProposal 数据类定义（owner 01，生产 04，消费 05）。
- 模块 05：service_execution 状态枚举补 WAITING_HUMAN/RETRY_WAIT；字段补 waiting_reason/human_deadline/requested_action/channel_source/retry_count/priority；新增 EXE-API-05（POST /executions/{id}/resume，decision=RESUME/CANCEL，超时幂等拒绝 EXECUTION_HUMAN_TIMEOUT）；修 content_hash 唯一约束范围。
- 模块 06：新增 RULE-WORK-06 资源治理（global/per-capability 并发、Resource Class 槽位、step timeout，总设 §5.2 P0 承接）与 RULE-WORK-07 人工等待（deadline 扫描复用 timer、HUMAN_TIMEOUT 单列统计）；补资源配置默认值表。
- 模块 07：新增 CAP-LIB-03/04 异步 Provider 合同（submit/status/result/cancel）；CAP-LIB-05 内置 Sandbox 工具注册（filesystem.read/write/edit/glob/grep、shell.execute，默认禁用）。
- 模块 08：skill_artifact 唯一约束改 (tenant_id, skill_id, checksum)。
- 模块 10：技术栈改 Node.js 20+/TypeScript/Fastify；新增 RULE-CHAN-06（Gateway 统一解析 IM 命令 /bind /skills /new /stop，仅转发不含业务语义）；新增 channel_delivery 表（投递重试唯一 owner=Worker，dedupe_key 防重）。
- 模块 15：新增 OPS-API-04 Console 概览聚合统计。
- 全库 8 个模块共 52 处"认证/授权：None"按 ADR-021 补齐（发布/用户/凭据/模型/平台/执行控制=Admin-only；配置开发类=Builder+Admin；内部接口=service token）；README 补全局鉴权 RULE 与新增接口 Owner 登记。

### 03-前端设计
- 10-执行记录：补 WAITING_HUMAN/RETRY_WAIT 状态、HumanApprovalPanel（waiting_reason/context/deadline 倒计时 + 继续/终止→EXE-API-05）、列表与详情补 trace_id/retry_count/channel_source/current_step_key。
- 09-用户管理：详情新增 Memory Tab（只读 + 单条删除 MEM-API-03），Playbook U06 承接。
- 90-Console交互规格：§9.1/9.2 对齐交互稿口径（接入渠道/当前阶段/追踪标识/人工审批区块）；§1.2 补"Admin-only 按钮对 Builder 不渲染"；新增 §1.3 Console 菜单清单（含审计查询）。
- 新增 11-审计查询/design-frontend.md（AUDIT-API-01，Admin-only）。
- 02-服务管理：Out of Scope 声明手工执行/回滚后置。

### 04-追溯与验收
- 01-矩阵升 V1.11：补模块 18/19、P05 承接改 18、新增 P23~P26（Human Checkpoint/资源治理/审计/Memory）。
- 02-矩阵升 V1.11：补概览（OPS-API-04）、人工审批（EXE-API-05）、投递诊断（channel_delivery）、Memory、审计查询行。
- 04-完整性检查：标注 V1.9.1 快照过期，字节数/行数刷新为 V1.11 实测，补 18/19，章节检查标记待重跑。

### docs/README.md
- 修正 99-cf-task-align模板基线 条目（目录不存在，指向 .code-flow/specs/shared/）。

## 补充裁决（同轮追加）

5. execution_type 枚举冻结为 `AGENTIC/DETERMINISTIC/HYBRID`（按"谁决定下一步"划分：运行时模型决策 / 预定义编排 / 混合；旧 AGENT/AUTOMATION/HYBRID 中 AUTOMATION 语义模糊弃用）。90 规格、FE-02 同步标注枚举冻结；05-清单为历史快照不改。
6. 公共字段不豁免：不可变表（service_release/execution_snapshot/task_progress_event/artifact/skill_artifact/skill_artifact_capability/message/audit_log，共 8 张）统一补 `update_time` 字段；全库删除"更新时间；不可变表除外"措辞。

## 追加执行（同轮完成）

- 命名漂移回写总设：impl_type→implementation_type、package_ref→artifact_ref、channel_message_ref→external_message_id、model_config_ref→model_config_id、release_id→release_no（FK 字段 service_release_id/agent_release_id 保持不变）；06-基线路径参数 {release_id}→{release_no}。
- 06-接口设计基线升 V1.12：补命名空间（/health /metrics）、全局鉴权说明、§8.1（CONV/MEM/AUDIT/OPS-API-04）、Execution API 补 resume、/agents/{id}/channel/wecom 标注 Owner=Channel Gateway、IM 四命令转发与 CH-INT-01 说明。

## 已知未闭合项（后续版本）

（前三项已于同轮完成，见下方"追加执行"；仅剩 align 重生成与流程 Gate。）

- 命名漂移对照表（model_config_ref/id、release_id/no、entity_type/resource_type 等）待统一发布。
- 不可变表缺 update_time 与公共字段规则的裁决待落（倾向允许不可变表省略）。
- 各模块追溯矩阵 off-by-one（LIB 接口未入矩阵）、B 场景缺失，待下一轮 align 重生成修复。
- 总设/Playbook 标题版本号（V1.8/V6）未升版，待下一次总设完整演进时统一。

## 追加修复：codex Review（2026-09-12，07-设计承接全面Review）"直接可修"项

依据 docs/04-追溯与验收/07-设计承接全面Review-2026-09-12.md（25 项发现全部核实属实），本批闭合其中 9 项直接可修项：

- R05：EXE-API-04 的错误码/处理逻辑误挂段移回原位，角色统一为仅 Admin；
- R10：12 个敏感端点权限重分为仅 Admin（CRED-API 全部、PLAT-API 写接口、MODEL-API-02/04/05、MEM-API-02/03），与 README 全局规则和 FE-09 对齐；
- R14：USR-API-01 的 user_key 改为服务端自动生成（可缺省）、PLAT-API-02 的 auth_type/auth_schema 改为创建后配置（可缺省），与交互稿/FE-08 对齐；
- R18：FE-10/90 规格状态枚举补全九态（PENDING/WAITING/CANCELLING，CANCELLED 拼写统一）；current_step 统一为 VARCHAR 步骤 key（模块 05/08 同步）；EXE-API-01 补 trace_id 筛选；FE-01 接入 OPS-API-04；audit_log 补 details/execution_id 字段与 AuditLogView DTO（无 execution_id 不显示跳转）；
- R19：short_page_terminates 默认 true 裁决回写 ADR-015、90 规格、FE-04 三处（与 06-基线统一）；
- R21：9 处复合唯一索引中的 is_deleted 列改为 partial unique predicate（含 channel_binding 的 ACTIVE 条件）；
- R22：18 个悬空场景 ID 补齐定义；两处语义错位引用修正（RULE-WORK-07→S-WORK-06 人工超时扫描、RULE-API-01→S-API-05 Control Plane Down），复核全库 dangling=0；
- R25：删除两份 %20 旧路径母本（与主文档在 Knowledge/幽灵表/Capability pin 三处冲突的未标历史副本）；
- 边界1：Console 登录合同回写（FE-00 §3.3.1 + 90 规格 §1.4，与已实现代码一致：login/logout/Bearer 12h/bootstrap CLI/角色来源）。

未闭合：R01-R04、R06-R09、R11-R13、R15-R17、R20、R23、R24 及边界 2-5（需设计补充或架构决策，见 review 修订次序建议）。

## 追加修复：第二批（设计补充）+ 第三批（架构裁决），2026-09-12

核实结论：剩余 13 项均无业务决策点（产品意图在总设/Playbook 已定），全部按最优架构方案裁决并落文档。

**新增 ADR（00-总体设计/03-核心设计决策.md）**：
- ADR-033 IM 命令走受信内部 Contract（CH-INT-02），不复用 Console API（R06）
- ADR-034 Skill SDK 同步门面 + 异步宿主桥接（R12）
- ADR-035 V1 生产沙箱 = 隔离子进程执行器（rlimit/只读挂载/进程组清理，容器化留 V2）（R24）
- ADR-036 外部 Session 过期（自动 refresh）与长期凭据失效（立即拒绝）分离（R11）
- ADR-037 Memory 由 Runtime 受控写入、用户经 IM `/memory list|clear` 自助治理（R16）
- ADR-038 失败重试 = 新建关联 Execution（parent_execution_id），沿用已成功步骤幂等键（R07）
- ADR-039 Draft 测试走草稿快照（execution_source=TEST，release 可空），正式执行强制 current release（R02）

**第二批设计合同（模块文档）**：
- R01：ExecutionProposal 补 input/confirmation_ref/confirmation_digest/created_at 与确认消费合同（摘要比对/TTL/幂等/防参数替换）
- R02：SVC-API-06 Draft 测试合同 + service_execution 补 execution_source/draft_revision 字段
- R03：模块 04/08/19 执行器接入 ExecutionProjection 冻结投影（checksum/model snapshot 优先，enabled 动态）
- R04：WORK-LIB-01 逐状态 claim 条件表（PENDING 无门槛/WAITING 类看 next_run_at/WAITING_HUMAN 看决策或 deadline/CANCELLING 必可领）+ NULL 语义 + 命令优先
- R06：模块 10 新增 CH-INT-02 内部命令 Contract（7 类命令、真实 END_USER 注入、归属校验、幂等）
- R07：EXE-API-04 重试合同（ADR-038 落地）+ service_execution 补 parent_execution_id
- R08：ServiceDraft Schema 冻结（五类 Step、${} 输入映射、失败策略、校验 RULE 与 422 错误码）
- R09：CH-INT-01 投递事务链（Worker 预建 delivery→单次发送→回写→有限重试；dedupe_key 含 route+event_seq）+ EXE-API-02 补 deliveries 投递诊断
- R16：模块 11 Memory 受控写入/自助治理合同 + MEM-INT-01 + S-MEM-06
- R12/R24/R11：模块 17/13/09 合同（见 ADR）

**验证**：架构守护测试 28 通过；全库场景 ID dangling=0。

**仍未闭合（第四批）**：R13（Skill 导入预览/确认分离）、R15（Artifact 用户交付路径）、R17（用户 revision 链路）、R18 深层（页面 DTO 全量渲染验证）、R20（Async Handle 合同细化）、R23（Checkpoint identity）、R22 深层（场景语义人工复核）、边界 2-5（紧急停用入口/跨语言 Gateway 通道/Gateway 扩容/治理口径）。

## 追加修复：第四批（合同补齐），2026-09-12

R13/R15/R17/R20/R23 与边界 2-5 全部落入设计文档（深层验证类 R18 页面渲染验证、R22 场景语义人工复核保留为编码期验收活动）：

- R13：SKILL-API-02/04 两阶段导入（mode=preview 暂存+preview_token → mode=commit 重新校验 checksum 原子落库；取消零残留、重复 commit 幂等）；
- R15：EXE-API-06 Artifact 授权下载（归属校验→短时签名 URL 302）；模块 14 sign_read 合同（仅模块 05/10 可签发）；模块 17 ArtifactClient write/read 合同（Workspace 内越界拒绝）；End User 侧经 DELIVERY 投递文件/签名链接；
- R17：platform_user 补 revision 列（BIGINT 默认 1），USR-API-03 返回 revision、编辑回传；grant 措辞修正为仅切换 enabled；
- R20：CAP-LIB-03/04 完整合同（submit 必传 idempotency_key；AsyncTaskHandle 可序列化=async_task_run 持久列；跨 Worker 由持久行重建 handle + 当前凭据；submit 结果未知置 SUBMITTED_UNKNOWN 走对账，不自动重发）；async_task_run 状态枚举补 SUBMITTED_UNKNOWN；
- R23：模块 11 当前会话映射（user×agent×channel 单 ACTIVE 部分唯一）/new 原子切换/会话行锁串行；Checkpoint Identity（Chat thread=conversation_id、Worker thread=execution_id、namespace=agent_id，Step 边界=checkpoint 边界）；模块 10 入站映射同步；
- 边界2：新增 SVC-API-10 紧急启停（PATCH /enabled，Admin，即时生效绕过 Draft，在途不中断，EXECUTION_SERVICE_DISABLED 409）；
- 边界3：模块 10 改为"无直连业务库"——数据经 platform-api 受信内部 REST；migration 唯一 Owner=Python Alembic；
- 边界4：RULE-CHAN-08 V1 单副本声明 + 多副本前 Gate（Redis 连接归属/错实例转发/降级）；
- 边界5：RULE-WORK-06 治理口径修正（global/timeout=P0；per-capability=进程内计数，集群级后续；Resource Class/priority=P1 提前纳入并标注）。

（本声明已被后续复审修正：见《08-V1.12.1-复审修复与收尾.md》——复审判定多数项为部分修复，V1.12.1 完成全部设计层闭合。）
