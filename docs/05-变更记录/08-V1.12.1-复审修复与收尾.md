# V1.12 → V1.12.1：复审修复与设计收尾

> 依据《07-设计承接全面Review》25 项发现与《08-设计承接修复复审》判定（4 闭合 / 21 部分 / N01 新增），本版完成全部设计层修复。运行验证（页面真实渲染、E2E）属编码期活动，不在本版范围。

## 复审优先项修复（产生错误运行行为的 6+1 项）

- R04：Worker claim 资格改为统一 SQL 契约（全局 lease 空/过期前置 + lease_epoch 版本）；CANCELLING/WAITING_HUMAN 领取保留状态且受统一租约约束。
- R09：投递改为"领取投递许可"模型（CH-DATA-03 attempt_token/epoch 原子登记，同 epoch 不再次许可；CH-DATA-04 Artifact 授权字节流；Worker outbox 走 Python Port 直连 PG）。
- R19：短页终止语义修正——"下游可能产生中间短页时必须允许显式关闭；仅当保证非末页必满才可安全依赖短页终止"，三处统一。
- R02：service_execution/execution_snapshot 增加 source(FORMAL/TEST) 判别与条件 CHECK（TEST 无 release 也可落库，draft_revision/test_mode 必填）。
- R20：async_task_run 增加 capability_key（恢复路由），external_task_id 条件非空（SUBMITTING/SUBMITTED_UNKNOWN/FAILED 可空，不得伪造）。
- R10：模型/平台读接口恢复 Builder+Admin（安全选项 DTO，无 Secret），写接口保持 Admin-only。
- N01：Gateway 运行面数据通路迁至 agent-runtime 承载（CH-DATA-01..04/CH-INT-02 装配 Python Channel/Conversation/Execution Application + PG Repository），全链不经过 platform-api；Gate H 冲突解除。

## 其余部分项收尾（复审第二批）

R01 提案签发/取回/确认绑定（Proposal Application + EXE-LIB-02）；R03 ExecutionProjection 结构化（projection.agents/skills + EXECUTION_PROJECTION_REQUIRED，Execution 不回退 current）；R05 RESUME/CANCEL 持久化与超时竞争裁决；R06 命令幂等改绑真实消息 ID + Chat Run 停止入口；R07 重试响应返回 new_execution_id + operation_id/幂等键沿用；R08 Worker WAIT 执行分支；R11 凭据与 Session 到期字段拆分；R13 导入判别 DTO（preview multipart / commit JSON + token，SKILL_PREVIEW_* 错误码）；R14 平台 UNCONFIGURED 未配置态；R15 End User /result 重取（ARTIFACT_EXPIRED 语义，不暴露 bearer URL）；R16 Memory 受控写工具 + 来源枚举统一；R18 delivery_status 筛选与 ExecutionView；R23 会话 ARCHIVED 状态 + checkpoint step_key 区分；R24 沙箱 seccomp/网络出站禁令。

## 本轮收尾（V1.12.1）

1. 《08-表所有权》索引同步新字段（source/test_mode/draft_revision/parent_execution_id、async_task_run.capability_key、platform_user.revision、audit_log.details/execution_id、execution_snapshot 条件约束）。
2. 02-README 全局鉴权 RULE 补登录豁免（POST /api/v1/auth/login）。
3. 12 份前端文档的通用"打开页面并完成主操作"场景全部替换为具体断言场景（含预览/提交、发布、审批闭环、动态表单、角色差异、乐观锁、两阶段导入等），R22 设计层缺口闭合。

## 完成声明（替代 07 号变更记录旧声明）

codex review 25 项发现 + 5 项边界 + N01：**设计文档层面全部闭合**。剩余为编码期验证活动：
- R18 深层：页面用真实 API 响应渲染验证；
- R22 深层：新增场景随实现执行；
- 04-文档完整性检查重跑、Spec Context 绑定（cf-task Gate）。
