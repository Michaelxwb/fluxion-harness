# 07 跨模块接口与协议详细设计

## 1. 统一协议约定

### 1.1 HTTP

- JSON：`application/json; charset=utf-8`；
- SSE：`text/event-stream`；
- 时间：RFC3339；
- ID：UUID string；
- Internal API 不对公网 Ingress 暴露；
- 服务间必须透传 `traceparent`，同时传业务关联头。

### 1.2 统一 Header

```text
X-Trace-Id
X-Request-Id
X-Caller-Service
X-Tenant-Id
```

Phase 1 防御性服务身份：内部 NetworkPolicy + 可选共享 `X-Service-Token`（来自 SecretProvider）；未来可升级 mTLS，不改变业务 Contract。

### 1.3 错误 Envelope

```json
{
  "error": {
    "code": "AGENT_ACCESS_DENIED",
    "message": "agent access denied",
    "details": {},
    "trace_id": "..."
  }
}
```

用户可见消息由上层映射，Internal API message 不作为最终 UI 文案。

---

## 2. IM Gateway -> Agent Runtime

### 2.1 `POST /v1/runs`

创建并流式执行一次 Run。

该接口部署在 `agent-runtime` Service 后面。调用方只传 `agent_id`，**不得传 runtime_pod_id**；负载均衡可把请求发到任意 Runtime Pod。

请求：

```json
{
  "agent_id": "uuid",
  "platform_user_id": "uuid",
  "conversation_id": "uuid-or-null",
  "channel": {
    "type": "WECOM",
    "bot_id": "bot_xxx",
    "external_conversation_id": "xxx"
  },
  "message": {
    "id": "msg_xxx",
    "type": "text",
    "text": "帮 A 客户做一次策略检查",
    "attachments": []
  }
}
```

响应：SSE。

首个事件：

```text
event: run.created
data: {"run_id":"...","conversation_id":"...","resumed":false,"trace_id":"..."}
```

调用语义（V1.4）：

- 同一 conversation 同时最多一个非终态 Run；
- 若存在 `WAITING_INPUT` Run，本请求被当作 resume 输入继续该 Run（`run.created` 带 `"resumed": true`）；
- 若存在 `CREATED/RUNNING` Run，返回 `409 RUN_BUSY`；
- 否则创建新 Run。

### 2.2 `POST /v1/runs/{run_id}/resume`

```json
{
  "input": {
    "type": "text",
    "text": "选择设备 1 和 2"
  }
}
```

约束：Run 必须为 `WAITING_INPUT`。IM 会话中的普通回复不需要 Gateway 显式调用本接口，Runtime 会在 `POST /v1/runs` 内自动 resume；本接口保留给显式恢复与诊断场景。

### 2.3 `POST /v1/runs/{run_id}/cancel`

幂等。已终态返回当前终态。取消为协作语义：`WAITING_INPUT` 直接置 `CANCELLED`；`RUNNING` 返回 `CANCELLING`，执行 Pod 协作停止后置 `CANCELLED`。

### 2.4 `POST /v1/runs/cancel-active`

```json
{
  "agent_id": "uuid",
  "platform_user_id": "uuid"
}
```

语义：

- 无活跃 Run → `404 NO_ACTIVE_RUN`；
- `WAITING_INPUT` → 直接 CAS 置 `CANCELLED`（无执行者）；
- `CREATED/RUNNING` → 写 `cancel_requested=true` + Redis hint `run:cancel:{run_id}`，响应 `{"run_id":"...","status":"CANCELLING"}`，执行 Pod 协作停止后置 `CANCELLED`；
- 用户提示为受理语义“正在停止当前任务…”。

### 2.5 `POST /v1/conversations`

```json
{
  "agent_id": "uuid",
  "platform_user_id": "uuid"
}
```

用于 `/new`。

### 2.6 `GET /v1/runs/{run_id}`

返回 Run 当前状态与 Snapshot 摘要引用（不含 Secret），用于 Gateway 诊断与 Console 详情。

---

## 3. SSE Event Contract

所有事件公共字段：

```json
{
  "run_id": "uuid",
  "seq": 12,
  "timestamp": "2026-09-17T11:00:00+08:00",
  "type": "message.delta",
  "data": {}
}
```

连接空闲时 Runtime 发送 SSE 注释帧 `: heartbeat`（不带 event 与 seq），Consumer 忽略即可。事件按 `seq` 单调有序。

### 3.1 `message.delta`

```json
{"delta":"正在查询客户设备..."}
```

### 3.2 `skill.loaded`

```json
{"skill_key":"policy-check","version":"1.3.0"}
```

### 3.3 `tool.started`

```json
{"tool_call_id":"tc_1","tool_name":"execute_skill"}
```

### 3.4 `tool.completed`

```json
{"tool_call_id":"tc_1","status":"SUCCESS","artifact_id":null}
```

### 3.5 `task.accepted`

```json
{
  "task_id":"uuid",
  "status":"QUEUED",
  "delivery_mode":"FINAL_ONLY",
  "message":"任务已受理，完成后会通知你"
}
```

### 3.6 `interrupt.required`

```json
{
  "interrupt_id":"uuid",
  "kind":"CONFIRM",
  "prompt":"将对 2 台设备执行策略检查，是否继续？",
  "options":["继续","取消"]
}
```

### 3.7 `artifact.created`

```json
{"artifact_id":"uuid","media_type":"application/json","preview":"..."}
```

### 3.8 `run.completed`

```json
{"status":"COMPLETED","final_text":"..."}
```

### 3.9 `run.failed`

```json
{"status":"FAILED","error_code":"MODEL_UNAVAILABLE"}
```

---

## 4. Agent Runtime -> Console Platform

### 4.1 `POST /internal/runtime/resolve-definition`

请求：

```json
{
  "agent_id":"uuid",
  "actor_user_id":"uuid",
  "channel":"WECOM"
}
```

响应：

```json
{
  "agent": {
    "id":"uuid",
    "key":"mss-agent",
    "revision":12,
    "instructions":"...",
    "runtime_config":{}
  },
  "model": {
    "id":"uuid",
    "revision":4,
    "protocol":"OPENAI",
    "model_id":"qwen3-235b-a22b",
    "base_url":"http://model-gateway.internal/v1",
    "secret_ref":"secret://model/xxx",
    "params":{}
  },
  "skills":[
    {
      "skill_id":"uuid",
      "artifact_id":"uuid",
      "key":"policy-check",
      "name":"设备策略检查",
      "description":"...",
      "version":"1.3.0",
      "checksum":"sha256:...",
      "storage_key":"skills/{skill_id}/{artifact_id}/skill.zip",
      "frontmatter":{},
      "execution_mode":"ASYNC"
    }
  ],
  "mcp_servers":[]
}
```


权限解析语义（完整判定见 00 §0.9 / 02 §5.10）：

```text
skills =
AgentAccessGrant(is_deleted=false)
∩ Agent.enabled
∩ AgentSkillBinding(is_deleted=false)
∩ Skill.enabled ∩ Skill.is_deleted=false
∩ Skill.user_scope(ALL/SELECTED + SkillUserGrant.is_deleted=false)

mcp_servers =
AgentAccessGrant(is_deleted=false)
∩ Agent.enabled
∩ AgentMcpBinding(is_deleted=false)
∩ MCP.enabled ∩ MCP.is_deleted=false
∩ MCP.user_scope(ALL/SELECTED + McpUserGrant.is_deleted=false)
```

因此响应里的 `skills/mcp_servers` 已是 Effective Capability，不是 Agent 的原始全量绑定。


错误：

```text
AGENT_NOT_FOUND
AGENT_DISABLED
AGENT_ACCESS_DENIED
MODEL_DISABLED
```

### 4.2 `POST /internal/runtime/resolve-egress-access`

该接口只做：

- ProjectPlatform 解析；
- `adapter_key/adapter_config` 返回；
- 凭据选择策略与 CredentialRef 解析；
- 调用授权/审计归因。

不返回 Secret Value，也不在 Console 层执行平台登录。

`execution_ref.type` 只允许 `RUN/TASK`；Runtime 用 RUN，Worker 用 TASK。`target.type` 与数据库审计统一为 `PLATFORM_SERVICE/HTTP/MCP`。

请求：

```json
{
  "actor_user_id":"uuid",
  "execution_ref":{"type":"RUN","id":"uuid"},
  "skill_artifact_id":"uuid",
  "platform_key":"mssw-prod",
  "target": {
    "type":"PLATFORM_SERVICE",
    "service":"customer-service-mgr",
    "operation":"get_customer"
  }
}
```

响应：

```json
{
  "decision":"ALLOW",
  "platform": {
    "id":"uuid",
    "key":"mssw-prod",
    "resolver_type":"BASE_URL",
    "resolver_config":{"base_url":"https://mssw.internal"},
    "adapter_key":"mssw",
    "adapter_config":{
      "login_endpoint":"/gateway/login_agent",
      "health_endpoint":"/v1/rtt"
    },
    "adapter_schema_version":"1",
    "credential_mode":"USER_THEN_SHARED"
  },
  "credential": {
    "ref_id":"uuid",
    "secret_ref":"secret://user/u1/mssw",
    "credential_schema_version":"1"
  }
}
```

Runtime/Worker 随后在本地共享 `platform-sdk` 中：

```text
AdapterRegistry.get(adapter_key)
 -> SecretProvider.get(secret_ref)
 -> PlatformSessionManager
 -> adapter.authenticate/validate/prepare_request
```

### 4.3 `GET /internal/platform-adapters/{adapter_key}`

Runtime/Worker 通常通过本地 `platform-sdk` Registry 获取 Adapter；该接口主要用于跨版本诊断和 Console 元数据一致性检查。

响应示例：

```json
{
  "key": "mssw",
  "version": "1",
  "session_mode": "SESSION",
  "platform_config_schema_version": "1",
  "credential_schema_version": "1"
}
```


## 5. Agent Runtime -> Agent Worker

这些 Internal API 只在内网开放。

### 5.1 `POST /internal/tasks`

创建即时 Background Task。通常由 Runtime 的 `execute_skill -> ExecutionRouter` 调用，而不是让 LLM 自己构造后台 Task。

请求：

```json
{
  "tenant_id":"t1",
  "agent_id":"uuid",
  "actor_user_id":"uuid",
  "source_run_id":"uuid",
  "intent_key":"policy_check",
  "skill_id":"uuid",
  "skill_artifact_id":"uuid",
  "input": {"customers":["A","B","C"]},
  "execution_snapshot": {},
  "snapshot_hash":"sha256:...",
  "idempotency_key":"run:tool-or-router-key",
  "delivery_route": {
    "channel":"WECOM",
    "bot_id":"bot_xxx",
    "external_user_id":"wotv...",
    "external_conversation_id":"xxx"
  },
  "delivery_mode":"FINAL_ONLY"
}
```

响应：

```json
{"task_id":"uuid","status":"QUEUED"}
```

### 5.2 `POST /internal/schedules`

创建定时任务。调用方必须已经由 Agent 理解并规范化业务意图。

```json
{
  "name":"每周策略检查",
  "agent_id":"uuid",
  "actor_user_id":"uuid",
  "intent_key":"policy_check",
  "skill_id":"uuid",
  "input_template":{"customer":"A"},
  "schedule":{"type":"CRON","cron":"0 9 * * 1","timezone":"Asia/Shanghai"},
  "delivery_route": {
    "channel":"WECOM",
    "bot_id":"bot_xxx",
    "external_user_id":"wotv..."
  }
}
```

Schedule 保存稳定 `skill_id`，不保存 Skill Artifact checksum。每次触发时按 `skill_id` 校验当前 Skill/Agent Binding/user_scope，再读取 current_artifact_id 生成新 Task Snapshot；不得按 skill_key 重新匹配资源。V1 错过触发按 SKIP 处理（不补发），无 misfire 配置。

### 5.3 Task 查询与取消

```text
GET  /internal/tasks/{task_id}
GET  /internal/tasks?schedule_id=&agent_id=&actor_user_id=&status=&trigger_type=&start_time=&end_time=&page=&page_size=
POST /internal/tasks/{task_id}/cancel

GET    /internal/schedules?actor_user_id=
PUT    /internal/schedules/{schedule_id}
DELETE /internal/schedules/{schedule_id}
```

更新 Schedule 仅影响未来触发。通过 Agent 调用时，Worker 必须校验 `actor_user_id` 是 Schedule owner；Console 管理接口按 Admin/Builder 权限控制。

---

## 6. Agent Worker -> Console Platform

Worker 在 Schedule 触发或需要刷新授权时调用同一 Definition Resolve：

```text
POST /internal/runtime/resolve-definition
```

必须重新校验：

- Agent enabled；
- AgentAccessGrant；
- Skill/MCP 当前是否仍绑定/启用；
- actor 当前是否仍满足 Skill/MCP `user_scope`（ALL/SELECTED + Grant）；
- 用户凭据在实际调用时是否仍有效。

---

## 7. Agent Worker -> IM Gateway

### 7.1 `POST /internal/deliveries`

```json
{
  "task_id":"uuid",
  "delivery_key":"task:uuid:final",
  "route":{
    "channel":"WECOM",
    "bot_id":"bot_xxx",
    "external_user_id":"wotv...",
    "external_conversation_id":"xxx"
  },
  "message":{
    "type":"text",
    "text":"任务已完成：成功 18，失败 2。"
  },
  "artifact_ids":["uuid"]
}
```

该 API 只做渠道发送，不重新做 Agent reasoning。IM Gateway 以 `delivery_key` 去重（Redis `delivery:dedupe:{delivery_key}` SET NX EX 7d），重复请求直接返回 200；Redis 不可用时按 at-least-once 发送。


## 8. IM Gateway -> Console Platform

### 8.1 `GET /internal/channel/bots`

查询启用的 Bot snapshot。

响应：

```json
{
  "revision":"hash-or-number",
  "items":[
    {
      "bot_account_id":"uuid",
      "bot_id":"bot_xxx",
      "secret_ref":"secret://wecom/bot_xxx",
      "agent_id":"uuid",
      "enabled":true
    }
  ]
}
```

### 8.2 `POST /internal/channel/resolve`

请求：

```json
{
  "channel":"WECOM",
  "bot_id":"bot_xxx",
  "external_user_id":"wotv..."
}
```

响应：

```json
{
  "bound":true,
  "platform_user_id":"uuid",
  "agent_id":"uuid",
  "authorized":true
}
```

未绑定：

```json
{"bound":false,"agent_id":"uuid","authorized":false}
```

### 8.3 `POST /internal/channel/bind`

```json
{
  "channel":"WECOM",
  "bot_id":"bot_xxx",
  "external_user_id":"wotv...",
  "bind_code":"ABC123"
}
```

成功：

```json
{"platform_user_id":"uuid","bound":true}
```

### 8.4 `GET /internal/channel/skills`

参数：`agent_id + platform_user_id`。

只返回当前 `platform_user_id + agent_id` 的 Effective Skill Catalog，不返回 SKILL.md 全文，也不泄露未授权 Skill 名称。

---

## 9. Console -> Runtime/Worker Admin API

### 9.1 `GET /internal/admin/runs`

Query：

```text
agent_id
user_id
skill_id
status
start_time
end_time
page
page_size
```

`page_size <= 100`。

### 9.2 `GET /internal/admin/runs/{run_id}`

返回：

- Run；
- Snapshot 摘要；
- Canonical Timeline；
- Tool Audit；
- Egress Audit；
- Model Invocation；
- Artifact 列表。

### 9.3 Memory

```text
GET    /internal/admin/users/{user_id}/memory
DELETE /internal/admin/users/{user_id}/memory/{memory_id}
```

### 9.4 Worker Admin

```text
GET  /internal/admin/tasks
GET  /internal/admin/tasks/{task_id}
POST /internal/admin/tasks/{task_id}/cancel
GET  /internal/admin/schedules
PUT  /internal/admin/schedules/{schedule_id}/pause
PUT  /internal/admin/schedules/{schedule_id}/resume
```

---

## 10. Console External REST API

Console Browser 只调用 `/api/v1/*`，不直接访问 `/internal/*`。

### 10.1 Agent

```text
GET    /api/v1/agents
POST   /api/v1/agents
GET    /api/v1/agents/{agent_id}
PUT    /api/v1/agents/{agent_id}
DELETE /api/v1/agents/{agent_id}
```

创建/更新必须显式传 `model_id`，不存在平台默认模型。更新传 `expected_revision`；冲突返回 `409 REVISION_CONFLICT`。

Agent 关系采用单关系事务：

```text
GET    /api/v1/agents/{agent_id}/skills
POST   /api/v1/agents/{agent_id}/skills/{skill_id}
DELETE /api/v1/agents/{agent_id}/skills/{skill_id}

GET    /api/v1/agents/{agent_id}/mcp-servers
POST   /api/v1/agents/{agent_id}/mcp-servers/{mcp_id}
DELETE /api/v1/agents/{agent_id}/mcp-servers/{mcp_id}

GET    /api/v1/agents/{agent_id}/users
POST   /api/v1/agents/{agent_id}/users/{user_id}
DELETE /api/v1/agents/{agent_id}/users/{user_id}

GET    /api/v1/agents/{agent_id}/channels
POST   /api/v1/agents/{agent_id}/channels
PUT    /api/v1/agents/{agent_id}/channels/{channel_account_id}
DELETE /api/v1/agents/{agent_id}/channels/{channel_account_id}
```

禁止用全量 PUT 覆盖整个关系集合。

### 10.2 Skill

```text
GET    /api/v1/skills
POST   /api/v1/skills/import
GET    /api/v1/skills/{skill_id}
PUT    /api/v1/skills/{skill_id}

GET    /api/v1/skills/{skill_id}/artifacts
POST   /api/v1/skills/{skill_id}/artifacts
GET    /api/v1/skills/{skill_id}/artifacts/{artifact_id}

PUT    /api/v1/skills/{skill_id}/user-scope
GET    /api/v1/skills/{skill_id}/users
POST   /api/v1/skills/{skill_id}/users/{user_id}
DELETE /api/v1/skills/{skill_id}/users/{user_id}
```

`POST /skills/import` = 新 Skill + 首个 Artifact；`POST /skills/{id}/artifacts` = 已有 Skill 导入新不可变版本。

Artifact Detail 返回 `artifact_id/version/checksum/storage_key/execution_mode/validation_status/manifest`。

### 10.3 MCP

```text
GET    /api/v1/mcp-servers
POST   /api/v1/mcp-servers
GET    /api/v1/mcp-servers/{mcp_id}
PUT    /api/v1/mcp-servers/{mcp_id}
DELETE /api/v1/mcp-servers/{mcp_id}

POST   /api/v1/mcp-servers/{mcp_id}/test
POST   /api/v1/mcp-servers/{mcp_id}/discover-tools
GET    /api/v1/mcp-servers/{mcp_id}/tools
GET    /api/v1/mcp-servers/{mcp_id}/tools/{tool_name}

PUT    /api/v1/mcp-servers/{mcp_id}/user-scope
GET    /api/v1/mcp-servers/{mcp_id}/users
POST   /api/v1/mcp-servers/{mcp_id}/users/{user_id}
DELETE /api/v1/mcp-servers/{mcp_id}/users/{user_id}
```

`test` 不修改 Tool Catalog；`discover-tools` 执行 `initialize + tools/list`，成功后更新 `tool_catalog_json/hash/revision/last_discovered_at`。V1 不提供 Tool 级 enabled API。

### 10.4 Model

```text
GET    /api/v1/models
POST   /api/v1/models
GET    /api/v1/models/{model_id}
PUT    /api/v1/models/{model_id}
DELETE /api/v1/models/{model_id}
POST   /api/v1/models/batch-test
```

字段：

```text
key / name / protocol=OPENAI / model_id / base_url / api_key / params / enabled
```

没有 `is_default`。

批量测试同步返回逐模型：

```json
{
  "items":[
    {"model_id":"uuid","status":"AVAILABLE","latency_ms":418,"error_code":null,"tested_at":"..."}
  ]
}
```

### 10.5 User

```text
GET    /api/v1/users
POST   /api/v1/users
GET    /api/v1/users/{user_id}
PUT    /api/v1/users/{user_id}

GET    /api/v1/users/{user_id}/agents
POST   /api/v1/users/{user_id}/agents/{agent_id}
DELETE /api/v1/users/{user_id}/agents/{agent_id}

GET    /api/v1/users/{user_id}/identities
POST   /api/v1/users/{user_id}/bind-codes
DELETE /api/v1/users/{user_id}/identities/{identity_id}

GET    /api/v1/users/{user_id}/memory
DELETE /api/v1/users/{user_id}/memory/{memory_id}
DELETE /api/v1/users/{user_id}/memory
```

最后一个 DELETE = 清理全部用户记忆。

### 10.6 PlatformAdapter Metadata

```text
GET /api/v1/platform-adapters
GET /api/v1/platform-adapters/{adapter_key}
```

### 10.7 ProjectPlatform

```text
GET    /api/v1/project-platforms
POST   /api/v1/project-platforms
GET    /api/v1/project-platforms/{platform_id}
PUT    /api/v1/project-platforms/{platform_id}
DELETE /api/v1/project-platforms/{platform_id}
POST   /api/v1/project-platforms/{platform_id}/test
```

如果 `adapter_key` 改变：

```text
user_credential_ref -> INVALID
shared_credential_ref -> INVALID
清理该 platform 的 PlatformSession
返回 credential_reconfigure_required=true
```

### 10.8 Project Credential

用户凭据：

```text
GET/PUT/DELETE /api/v1/project-platforms/{platform_id}/users/{user_id}/credential
```

共享凭据：

```text
GET/PUT/DELETE /api/v1/project-platforms/{platform_id}/shared-credential
```

V1 每个平台最多一套共享凭据。

### 10.9 Background Task

```text
GET  /api/v1/tasks
GET  /api/v1/tasks/{task_id}
POST /api/v1/tasks/{task_id}/cancel
```

Query：

```text
schedule_id / agent_id / actor_user_id / status / trigger_type / start_time / end_time / page / page_size
```

Schedule 历史触发直接查询 `GET /api/v1/tasks?schedule_id=...`。

### 10.10 Schedule

```text
GET    /api/v1/schedules
GET    /api/v1/schedules/{schedule_id}
PUT    /api/v1/schedules/{schedule_id}/pause
PUT    /api/v1/schedules/{schedule_id}/resume
DELETE /api/v1/schedules/{schedule_id}
```

内部创建必须包含 `name/agent_id/actor_user_id/intent_key/skill_id/input_template/schedule/delivery_route`。

ONCE 成功创建唯一 TaskExecution 后状态进入 `COMPLETED`。

### 10.11 Audit

```text
GET /api/v1/audits
GET /api/v1/audits/{audit_id}
```

Query 至少支持 `resource_type/resource_id/actor_user_id/action/result_status/trace_id/start_time/end_time/page/page_size`。

`result_status` 为统一执行结果字段：`SUCCESS | FAILED | <领域错误码>`；映射规则为 `egress_audit.result_status`、`tool_call_audit.status`、`model_invocation_audit.status`，配置变更审计固定为 `SUCCESS`。

Console 不提供图形化 Task/Workflow 编排器。


## 11. 分页 Envelope

```json
{
  "data": {
    "items": [],
    "page": 1,
    "page_size": 20,
    "total": 123
  }
}
```

约束：

```text
page 从 1 开始
page_size 默认 20
page_size 最大 100
```

---

## 12. 幂等

以下接口支持 `Idempotency-Key`：

- `/v1/runs`（以 channel message id 为 key）；
- `/internal/channel/bind`；
- `/api/v1/skills/import`、`/api/v1/skills/{skill_id}/artifacts`（checksum 可辅助去重）；
- `/internal/tasks`；
- `/internal/schedules`；
- `/internal/deliveries`（以 `delivery_key` 去重）；
- 未来高风险写接口。

---

## 13. 版本兼容

Internal Contract 变更规则：

- 新增 optional 字段：兼容；
- 删除/重命名：至少跨一个版本；
- enum 新增值：Consumer 必须 unknown-safe；
- Snapshot Schema 增加 `schema_version`；
- API path 重大破坏性变化进入 `/v2`。
