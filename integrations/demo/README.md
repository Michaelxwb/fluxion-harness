# Demo Integration

A second, non-MSS integration exists to prove that Framework Core abstractions are not accidentally MSS-specific.

The first implementation can be intentionally tiny, for example:

- Agent: Demo Assistant
- Service: Echo Report
- Capability: `demo.echo`
- Auth: NoAuthProvider
- Knowledge: in-memory/static demo provider
