# MSS Reference Integration

MSS is the first real-world integration used to validate the framework. It is intentionally separated from `framework/**`.

Expected content later:

```text
services/
agents/
capabilities/
auth/
knowledge/
tests/
```

Do not import this package from Framework Core.
